# Ergo.Chat Website

## Development

1. Install [Hugo](https://gohugo.io/) (tested with v0.158.0, YMMV)
2. `hugo build` builds the site in `public/`
3. For testing purposes, serve with `hugo server` (loopback-only by default, add `--bind 0.0.0.0` if necessary)
